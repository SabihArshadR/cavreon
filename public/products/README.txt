Drop real product photos in this folder, then point to them from
src/data/products.ts using the `image` field, e.g.

  { slug: "apex-95", ..., image: "/products/apex-95.jpg" }

Recommended: 1200 x 1200 px, product centred, plain background.
Any product without an `image` uses the built-in technical drawing.
